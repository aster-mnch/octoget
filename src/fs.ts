export async function mkdir(
  dir: string,
  options?: {
    force?: boolean;
    relativePath?: string;
  },
) {
  // TODO
}

export async function writeFile(
  path: string,
  content: string,
  options?: {
    force?: boolean;
    relativePath?: string;
  },
) {
  // TODO
}
